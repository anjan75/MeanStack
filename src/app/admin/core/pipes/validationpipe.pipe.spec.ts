import { ValidationpipePipe } from './validationpipe.pipe';

describe('ValidationpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationpipePipe();
    expect(pipe).toBeTruthy();
  });
});
